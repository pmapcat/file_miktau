(ns miktau.various-test
  (:require-macros [cljs.test :refer [deftest testing is]])
  (:require [cljs.test :as t]
            [re-frame.core :as refe]))


(deftest testing-subscription-should-work
  []
  ;; (is (= @(refe/subscribe [:selection-cloud]) ))
  ;; (is (= @(refe/subscribe [:cloud])))
  ;; (is (= @(refe/subscribe [:calendar]) ))
  ;; (is @(refe/subscribe [:fast-access-calendar]))
  ;; (is @(refe/subscribe [:nodes-changing]))
  ;; (is @(refe/subscribe [:node-items]))
  ;; (is @(refe/subscribe [:filtering]))
  
  )
